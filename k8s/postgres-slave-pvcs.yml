apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-slave1-pvc
  namespace: obscura-messenger
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-slave2-pvc
  namespace: obscura-messenger
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi